[pytest]
pythonpath = .
testpaths = tests evals
markers =
    eval: agent-quality evaluation against the golden dataset (distinct from tests/'s routing checks)
    costly: requires a real LLM judge call — excluded by default
addopts = -m "not costly"
